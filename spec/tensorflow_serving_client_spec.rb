require "spec_helper"

describe TensorflowServingClient do
  it "has a version number" do
    expect(TensorflowServingClient::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
